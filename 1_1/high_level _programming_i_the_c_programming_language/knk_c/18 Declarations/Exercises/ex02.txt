extern = used primarily to indicate that a variable or function can be shared by several files
static = shared by several functions in one file but hidden from functions in other files
extern & static = affect the storage duration of a variable